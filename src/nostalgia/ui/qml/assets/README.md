# Ảnh dùng trong giao diện

`hero.jpg` — ảnh làng Minecraft do chủ dự án cung cấp (tạo bằng công cụ sinh ảnh, gốc
1264×843), đã cắt về tỉ lệ 2:1 (bỏ 120 px trời phía trên và phần sân phía dưới) để giữ trọn
sáu công trình mà các thẻ nổi ở `HeroPanel.qml` neo vào.

Toạ độ công trình trong `HeroPanel.qml` đo trên ảnh đã cắt này; đổi ảnh là phải đo lại.
