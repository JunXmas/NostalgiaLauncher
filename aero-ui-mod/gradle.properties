org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Fabric — build CHUẨN cho 1.21.11 chạy trên runtime intermediary (đúng thứ mà
# fabric-loader thật dùng). Dev map = official Mojang (source vẫn viết tên Identifier/
# PackRepository), còn jar production được loom remap sang intermediary + sinh refmap
# cho Mixin. TRƯỚC ĐÂY dùng era "26.2 / official namespace" (không có intermediary):
# jar ra tên-Mojang nên CRASH lúc chạy trên instance Fabric intermediary (class_310).
minecraft_version=1.21.11
loader_version=0.19.3
loom_version=1.17.19

# Mod
version=1.0.0
group=com.nostalgia.aeroui

# Fabric API — bản intermediary cho 1.21.11 (khớp mod thật trong instance FO 1.21.11).
fabric_api_version=0.141.6+1.21.11
